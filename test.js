    //  console.log(sitemap.html());
    //  console.log(sitemap.text());

    // const output = sitemap.find('loc').text();
    //  console.log(output);

           // const loc = $(el).children('loc');
            // console.log(loc.text());

            // const lastmod = $(el).children('lastmod');
            // console.log(lastmod.text());